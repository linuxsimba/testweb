# Test web using Node.js

Create a website that prints out the hostname of the docker container.

Not sure yet how to copy the hypervisor hostname to the docker container..
For now it is set via the docker-compose calls
